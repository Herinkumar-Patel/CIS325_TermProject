taskbridge.db

Table: tasks

columns: id, title, description, course, dueDate, priority, status, createdAt

JSON format

{
  id: int,
  title: string,
  description: string,
  course: string,
  dueDate: string,
  priority: string,
  status: string,
  createdAt: string
}
